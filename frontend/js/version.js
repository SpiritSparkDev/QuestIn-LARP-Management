// App version shown in the sidebar footer. Keep in sync with package.json.
// Derived from the commit history (Conventional Commits, one release per
// commit day starting at 0.1.0): a day with a `feat:` commit bumps MINOR,
// a day with only `fix:` commits bumps PATCH. Stays 0.x until a 1.0 release.
export const APP_VERSION = '0.26.0';
